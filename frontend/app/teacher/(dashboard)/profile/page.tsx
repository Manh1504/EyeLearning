// app/teacher/(dashboard)/profile/page.tsx — Hồ sơ giảng viên.
import ProfilePage from '@/components/profile/profile-page';

export default function TeacherProfilePage() {
  return <ProfilePage role="teacher" />;
}
